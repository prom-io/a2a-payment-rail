[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
gas_reports = ["EscrowHub", "VerdictIntegration"]

[profile.ci]
fuzz_runs = 1000
verbosity = 2

[profile.gas]
optimizer_runs = 200
gas_reports = ["EscrowHub", "VerdictIntegration"]
